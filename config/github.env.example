# Reference-only placeholder retained from the Milestone 0 design.
# `hermes-connect` does NOT create or consume config/github.env and no real
# value belongs here. GitHub CLI authentication is performed with HTTPS via
# `gh auth login` and `gh auth setup-git`; persistent state lives under
# `/opt/hermes-data/auth/github/` as documented in SECURITY.md §3.
#
# REPLACE_ME

# Template for the real config/openai.env, which is created by
# `hermes-connect` (Milestone 2) on the target instance and is git-ignored.
# Do not put a real key/session value in this .example file — it is
# committed to Git.

OPENAI_API_KEY=REPLACE_ME
OPENAI_ORG_ID=REPLACE_ME

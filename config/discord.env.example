# Template for the real config/discord.env, which is created by
# `hermes-connect` (Milestone 2) on the target instance and is git-ignored.
# Do not put a real token in this .example file — it is committed to Git.

DISCORD_BOT_TOKEN=REPLACE_ME
DISCORD_APPLICATION_ID=REPLACE_ME
DISCORD_GUILD_ID=REPLACE_ME

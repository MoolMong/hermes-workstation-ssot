# docker/

Placeholder directory. Not implemented yet — Milestone 0 only creates this
file to make the target repository tree traceable (see `ARCHITECTURE.md`
§6). No `Dockerfile` or `docker-compose.yml` exists here yet.

Planned contents (Milestone 1):

- `Dockerfile` — the Hermes container image definition.
- `docker-compose.yml` — local/instance container orchestration for
  Hermes and its Gateway.

Do not add functional code here before Milestone 1 is in progress. See
`MILESTONES.md`.
